name: Build OCI Image

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Install Nix
        uses: cachix/install-nix-action@v31
        with:
          github_access_token: ${{ secrets.GITHUB_TOKEN }}
          extra_nix_config: |
            experimental-features = nix-command flakes

      - name: Build
        run: nix build -L

      - name: Flake check
        run: nix flake check -L

      - name: Log in to GitHub Container Registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Get image name from Docker
        if: github.event_name != 'pull_request'
        id: image_meta
        run: |
          IMAGE_NAME=$(docker load < result 2>&1 | grep "Loaded image:" | awk '{print $NF}')
          echo "local_image=${IMAGE_NAME}" >> $GITHUB_OUTPUT

      - name: Tag and Push Image
        if: github.event_name != 'pull_request'
        run: |
          TARGET_IMAGE="ghcr.io/${{ github.repository }}"
          LOCAL_IMAGE="${{ steps.image_meta.outputs.local_image }}"
          # Actual upstream version, read from the flake's version output
          VERSION="$(cat "$(nix build --no-link --print-out-paths .#packages.x86_64-linux.version)")"

          docker tag "${LOCAL_IMAGE}" "${TARGET_IMAGE}:${VERSION}"
          docker tag "${LOCAL_IMAGE}" "${TARGET_IMAGE}:latest"

          docker push "${TARGET_IMAGE}:${VERSION}"
          docker push "${TARGET_IMAGE}:latest"
